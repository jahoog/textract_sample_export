# SAMPLE TO EXPORT DATA FROM TEXTRACT
This code will take PDFS and extracts tables/forms/queries as CSV files

It requires:
- Python
- AWS boto3 (and an AWS configured profile)
- The trp and trp2 packages

To run, simply type: python process.py
